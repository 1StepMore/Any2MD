# Evidence: Task 6 - Update convert_file() to accept mode parameter

## Date: 2026-05-05

## Changes Made

### pipeline.py - convert_file() signature updated

**Before:**
```python
def convert_file(input_path: Path) -> Path:
```

**After:**
```python
def convert_file(input_path: Path, mode: str = "fast") -> Path:
```

### Dispatcher instantiation updated

**Before:**
```python
dispatcher = Dispatcher()
```

**After:**
```python
dispatcher = Dispatcher(mode=mode)
```

## Verification

- Syntax check passed: `python3 -m py_compile pipeline.py`
- Function signature: `(input_path: Path, mode: str = "fast") -> Path`
- Mode is passed to Dispatcher which already accepts `mode` parameter (see dispatcher.py line 57)

## Preserved Behavior

- File existence check unchanged
- File size check unchanged (50MB MAX_FILE_SIZE)
- Cleaner.clean() call unchanged
- _get_unique_output_path() helper unchanged
- Output path logic unchanged